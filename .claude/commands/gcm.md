---
description: Suggest a commit message in one line
---

Review the staged diff (`git diff --cached`) and suggest a single-line conventional commit message (type: subject) under 72 characters. Output only the message, no explanation.
